name: example
description: An example Flutter project for adblocker webview
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: 3.5.0

dependencies:
  flutter:
    sdk: flutter

  adblocker_webview:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
